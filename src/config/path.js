
const protocol = "http";
const hostname = "localhost";
const port = 8880;

export const RestUrls = {
userProjetUrl: `${protocol}://${hostname}:${port}/api/userprojet`,
userInfosUrl: `${protocol}://${hostname}:${port}/api/usersinfos`,
}